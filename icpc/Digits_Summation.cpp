#include<bits/stdc++.h>
using namespace std;
int main() {
    long long int X, Y, M, N, Sum;
    cin >> X >> Y;
    M = (X % 10);
    N = (Y % 10);
    Sum = (M + N);
 cout << Sum << '\n';
    return 0;
}